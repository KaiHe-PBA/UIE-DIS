- [x] 学生模型接收 `x_t`、`y`、`t` 三类输入，并明确输出 `x_0`
- [x] 方案包含独立 `condition encoder` 且提供多尺度条件特征
- [x] encoder-decoder 各尺度均定义了显式条件注入机制
- [x] 方案包含显式 `time embedding` 设计与注入说明
- [x] 默认配置下给出轻量化约束与参数量控制思路，目标优先控制在 3M 以内
- [x] 方案包含至少一种针对水下退化的轻量结构改造，并解释原因
- [x] 输出包含模块级设计，说明主要类、层级关系、维度流与 `forward` 逻辑
- [x] 输出包含完整 PyTorch 代码骨架，覆盖 `model`、`dataset`、`loss`、`train`、`eval`、`utils`
- [x] 损失定义至少包含 consistency loss、重建损失与结构约束
- [x] 方案说明了感知损失、颜色损失、边缘损失或频域损失的可选启用条件
- [x] 训练流程说明包含数据接口、时间步采样、训练循环、验证循环与 checkpoint 逻辑
- [x] 推理流程说明覆盖一步采样与少步采样，并说明一致性蒸馏场景下的使用方式
- [x] 输出语言、命名风格与注释要求符合中文规格与工程可读性要求
